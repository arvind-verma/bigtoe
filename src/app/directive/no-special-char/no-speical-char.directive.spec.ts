import { NoSpeicalCharDirective } from './no-speical-char.directive';

describe('NoSpeicalCharDirective', () => {
  it('should create an instance', () => {
    const directive = new NoSpeicalCharDirective();
    expect(directive).toBeTruthy();
  });
});
